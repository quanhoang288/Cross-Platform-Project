import React from 'react';
import PropTypes from 'prop-types';

const Modal = props => {
    return (
        <div>
            
        </div>
    );
};

Modal.propTypes = {
    
};

export default Modal;