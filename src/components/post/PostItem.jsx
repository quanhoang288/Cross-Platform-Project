import React from 'react';
import PropTypes from 'prop-types';

const PostItem = props => {
    return (
        <div>
            
        </div>
    );
};

PostItem.propTypes = {
    
};

export default PostItem;