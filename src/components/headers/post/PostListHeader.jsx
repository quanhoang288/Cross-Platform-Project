import React from 'react';
import { Icon } from 'react-native-elements';

const PostListHeader = props => {
    return (
        <Icon type='simple-line-icon' name='social-github' size={36}/>
    );
};

PostListHeader.propTypes = {
    
};

export default PostListHeader;